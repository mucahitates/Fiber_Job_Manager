<Window x:Class="FiberJobManager.Desktop.Views.TempDocumentsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Hazırlama Evrakları (Temp)"
        Height="520" Width="900"
        WindowStartupLocation="CenterScreen"
        Background="#EEF2FF">

    <Grid Margin="14">

        <!-- OUTER CARD -->
        <Border Background="#F8FAFF"
                CornerRadius="18"
                Padding="18"
                BorderBrush="#CBD5E1"
                BorderThickness="2">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- HEADER -->
                <StackPanel>
                    <TextBlock Text="Hazırlama Evrakları"
                               FontSize="20"
                               FontWeight="Bold"
                               Foreground="#111827" />

                    <TextBlock Text="(DWT — DWG — LSP) Firmalara ait dosyalar"
                               FontSize="14"
                               FontWeight="SemiBold"
                               Margin="0,5,0,0"
                               Foreground="#475569"/>
                </StackPanel>

                <!-- INNER CARD -->
                <Border Grid.Row="1"
                        Background="#F1F5FF"
                        CornerRadius="14"
                        Padding="10"
                        Margin="0,14,0,0"
                        BorderBrush="#CBD5E1"
                        BorderThickness="2">

                    <DataGrid x:Name="dgTempDocs"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              IsReadOnly="True"

                              CanUserReorderColumns="False"
                              CanUserResizeColumns="False"

                              HeadersVisibility="Column"
                              GridLinesVisibility="All"
                              RowHeaderWidth="0"
                              RowHeight="42"

                              Background="Transparent"
                              RowBackground="#F8FAFF"
                              AlternatingRowBackground="#F1F5FF"
                              Foreground="#111827"
                              BorderThickness="0"
                              HorizontalGridLinesBrush="#CBD5E1"
                              VerticalGridLinesBrush="#CBD5E1"

                              

                              PreviewMouseLeftButtonDown="dgTempDocs_PreviewMouseLeftButtonDown">

                        <!-- STYLES -->
                        <DataGrid.Resources>

                            <!-- 📌 TEXT ORTALAMA -->
                            <Style TargetType="TextBlock" x:Key="CenterText">
                                <Setter Property="TextAlignment" Value="Center"/>
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="FontSize" Value="15"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                            </Style>

                            <!-- 📌 BAŞLIK -->
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="HorizontalContentAlignment" Value="Center"/>
                                <Setter Property="VerticalContentAlignment" Value="Center"/>
                                <Setter Property="Background" Value="#E0E7FF"/>
                                <Setter Property="Foreground" Value="#1F2937"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="FontSize" Value="16"/>
                                <Setter Property="Padding" Value="12,10"/>
                                <Setter Property="BorderThickness" Value="1"/>
                                <Setter Property="BorderBrush" Value="#CBD5E1"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#E7EDFF"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>

                            <!-- 📌 SATIR — SEÇİLMESİN -->
                            <Style x:Key="NoSelectionRowStyle" TargetType="DataGridRow">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Foreground" Value="#111827"/>
                                <Style.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="Transparent"/>
                                        <Setter Property="Foreground" Value="#111827"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>

                            <!-- 📌 HÜCRE — SEÇİLMESİN + HOVER -->
                            <Style x:Key="NoSelectionCellStyle" TargetType="DataGridCell">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Foreground" Value="#111827"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#E8EEFF"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="Transparent"/>
                                        <Setter Property="Foreground" Value="#111827"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>

                            <!-- 📌 BUTON -->
                            <Style TargetType="Button" x:Key="PrimaryBtn">
                                <Setter Property="Background" Value="#4F46E5"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="Padding" Value="8,3"/>
                                <Setter Property="Cursor" Value="Hand"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="Width" Value="80"/>
                                <Setter Property="Height" Value="28"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="Focusable" Value="False"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}"
                                                    CornerRadius="8">
                                                <ContentPresenter HorizontalAlignment="Center"
                                                                  VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#6366F1"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>

                        </DataGrid.Resources>

                        <!-- COLUMNS -->
                        <DataGrid.Columns>

                            <DataGridTextColumn Header="Dosya Adı"
                                                Binding="{Binding FileName}"
                                                Width="*"
                                                ElementStyle="{StaticResource CenterText}" />

                            <DataGridTextColumn Header="Firma"
                                                Binding="{Binding Firma}"
                                                Width="220"
                                                ElementStyle="{StaticResource CenterText}" />

                            <DataGridTextColumn Header="Proje Türü"
                                                Binding="{Binding ProjeTuru}"
                                                Width="220"
                                                ElementStyle="{StaticResource CenterText}" />

                            <DataGridTemplateColumn Header="İndir" Width="120">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="İndir"
                                                Style="{StaticResource PrimaryBtn}"
                                                Click="BtnDownload_Click"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                        </DataGrid.Columns>

                    </DataGrid>
                </Border>

            </Grid>
        </Border>

    </Grid>
</Window>
